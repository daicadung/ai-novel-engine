{
  "name": "@ai-novel-engine/llm-gateway",
  "version": "1.0.0",
  "description": "Provider-independent LLM Gateway abstraction",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
