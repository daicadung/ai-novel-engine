import type {Metadata} from "next"; export const metadata:Metadata={title:"AI Novel Engine",description:"Long-form AI novel generation platform"}; export default function Layout({children}:{children:React.ReactNode}){return <html lang="vi"><body style={{margin:0,fontFamily:"system-ui,sans-serif"}}>{children}</body></html>}
